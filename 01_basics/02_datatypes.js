"use strict"    // treat all JS Code as newer version of JS

// alert(3 + 3)  // we are using nodejs and not browsers

// Datatypes
let name = "Ayush" // String
let age = 20  // Number    number => 2^53 - 1 , bigint is for bigger than number
let isMale = true  // Boolean
// null  => standalone value 
// undefined => empty value and valye is not assigned
// symbol => unique value

// object 

console.log(typeof age);
console.log(typeof null);  // Null is an object
console.log(typeof undefined); // Undefined is undefined